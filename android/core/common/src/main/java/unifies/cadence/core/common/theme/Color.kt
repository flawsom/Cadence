package unifies.cadence.core.common.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────────────────────────
val CadenceTerracotta = Color(0xFFB5533C)      // Seed / primary
val CadenceTerracottaDark = Color(0xFFD4714E)  // Primary for dark theme
val CadenceTerracottaLight = Color(0xFFE89A7A) // Tonal variant
val CadenceEmber = Color(0xFFE85A2A)           // Accent / CTA

// ── Surfaces ───────────────────────────────────────────────────────────────
val SurfaceLight = Color(0xFFFDFCFA)           // Warm off-white
val SurfaceDark = Color(0xFF1A1614)            // Warm near-black
val SurfaceVariantLight = Color(0xFFF5F0EB)    // Card / secondary surface
val SurfaceVariantDark = Color(0xFF2A2420)     // Dark card surface

// ── Semantic: Status ───────────────────────────────────────────────────────
val StatusOnTrack = Color(0xFF3F6F52)          // Green — on track
val StatusBehind = Color(0xFFA4772A)           // Amber — behind (never red)
val StatusReviewDue = Color(0xFF7B61C4)        // Purple — review due
val StatusCompleted = Color(0xFF3F6F52)        // Green — completed

// ── Level badges ───────────────────────────────────────────────────────────
val LevelFoundations = Color(0xFF4A90A4)       // Blue — Foundations
val LevelCore = Color(0xFFA4772A)              // Amber — Core
val LevelAdvanced = Color(0xFF8B4A6B)          // Mauve — Advanced

// ── Dark theme overlays ────────────────────────────────────────────────────
val OverlayScrim = Color(0x66000000)           // Modal overlay
